To run the files locally change the IP and port arguments in shell script for respective machine.

To run the files on multiple machines compile and run the file on respective machine.

Required number of machines:-

1 machine for Monitor
3 for 3 Master Servers
6 for 6 Chunk Servers
Number of clients >= 0